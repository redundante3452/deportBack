services:
  postgres:
    image: postgres:16-alpine
    container_name: deport-postgres
    restart: unless-stopped
    env_file:
      - .env.local
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
