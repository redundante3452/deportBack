# Plantilla. Copiar a k8s/secret.yaml, poner los valores reales de OCI Database
# with PostgreSQL, y NUNCA commitear ese archivo (ya está en .gitignore).
apiVersion: v1
kind: Secret
metadata:
  name: deport-back-db
  namespace: deport-back
type: Opaque
stringData:
  DB_HOST: "CAMBIAR-endpoint-de-oci-database-with-postgresql"
  DB_USERNAME: "CAMBIAR"
  DB_PASSWORD: "CAMBIAR"
  DB_NAME: "CAMBIAR"
